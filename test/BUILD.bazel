load("//rules:qemu_output_test.bzl", "qemu_output_test")

qemu_output_test(
    name = "hello_semihosting_test",
    size = "small",
    srcs = ["hello_semihosting.cpp"],
    expected_output = "hello_semihosting.stdout",
    platform = "//platform:lm3s6965evb",
)
