load("//board:defs.bzl", "BOARDS")
load("//rules:qemu_runner.bzl", "qemu_runner")

[
    qemu_runner(
        name = "{}_runner".format(board.name),
        machine = board.name,
    )
    for board in BOARDS
]
